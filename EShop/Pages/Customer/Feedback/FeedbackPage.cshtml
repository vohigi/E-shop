@page
@model EShop.Pages.Feedback.FeedbackPage

@{
    ViewData["Title"] = "Зворотній зв'язок";
}

<h1>Форма зворотнього зв'язку</h1>
<hr />
<div class="row">
    <div class="col-md-4">
    <partial name="_StatusMessage" model="Model.StatusMessage" />
        <form method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.MessageHeader" class="control-label">Тема</label>
                <input asp-for="Input.MessageHeader" class="form-control" placeholder="Коротко опишіть суть повідомлення"/>
                <span asp-validation-for="Input.MessageHeader" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.MessageText" class="control-label">Повідомлення</label>
                <input asp-for="Input.MessageText" class="form-control" placeholder="Напишіть повідомлення тут"/>
                <span asp-validation-for="Input.MessageText" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Надіслати" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="/Index">До головного меню</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}